/**
 * プロジェクト文書(docs/)のひな型の正本。
 *
 * 3 種類の文書を slnmix が既定のひな型として内蔵する(フェーズ 6、設計書 §18)。
 *
 * - design  設計書: 開発の進め方と判断の根拠。確認事項がゼロになるまで
 *           AI と質疑応答で仕上げ、必須の確認事項がなくなったら実装に進む
 * - spec    仕様書: 実装済みの振る舞いの正本。誰が読んでも分かる書き方を求める
 * - handoff 引継ぎ書: チャット間の状態のみ(1 ファイル固定、毎回書き直す)
 *
 * ひな型は本文用テキスト(.prompt.md)の <template> として AI に渡す。
 * 文書を新しく作るとき AI はこの章立てに従う。ルート直下の
 * docs/templates/<kind>.md があればその内容で置き換える(procedure.md と同じ扱い)。
 *
 * Mermaid(flowchart / stateDiagram / erDiagram)を章立てに組み込み、md ビューアで
 * 処理の流れが図として読めるようにしている。HTML コメントは AI 向けの記入指示で、
 * 完成した文書には残さない(ひな型の末尾に明記)。
 *
 * 文面を変えたら DOC_TEMPLATES_VERSION を上げ、test-fixtures/docs/ の
 * スナップショットを更新すること。
 */

export const DOC_TEMPLATES_VERSION = 2;

export const DOC_KINDS = ["design", "spec", "handoff"] as const;
export type DocKind = (typeof DOC_KINDS)[number];

export function isDocKind(value: string): value is DocKind {
	return (DOC_KINDS as readonly string[]).includes(value);
}

/** 文書種別の日本語名(表示・<doc> の説明用) */
export const DOC_KIND_LABELS: Readonly<Record<DocKind, string>> = {
	design: "設計書",
	spec: "仕様書",
	handoff: "引継ぎ書",
};

/**
 * 設計書の 1 行目に置く状態行。slnmix はこの行だけを読んでモードを決める。
 * blocking = 必須(回答がないと実装に進めない)の未回答数、
 * deferred = 後回し(実装を進めながら期限までに回答)の未回答数。
 */
export const DESIGN_STATUS_LINE_EXAMPLE =
	"<!-- slnmix design: status=draft blocking=0 deferred=0 -->";

const DESIGN_TEMPLATE = `<!-- slnmix design: status=draft blocking=0 deferred=0 -->
<!-- 上の状態行は必ずファイルの 1 行目に置き、確認事項(§10)の未回答数と常に一致させる。
     blocking = 種別「必須」の未回答数、deferred = 種別「後回し」の未回答数。
     blocking が 0 になったら status=ready に変える(deferred が残っていても実装に進める)。 -->
# <画面名または機能名> 設計書 v0.1

作成日: YYYY/MM/DD
対象: <ソリューション名 / プロジェクト名>
状態: draft(必須の確認事項が残っている)
<!-- 状態は draft / ready のどちらか。状態行と一致させる -->

---

## 0. この文書の位置づけ

- 手本にする既存の画面・モジュールと、そこから踏襲するもの(SQL の書き方・ファイル構成・命名・エラー処理・監査列)
- この設計書で決めること / 決めないこと

### 0-1. 受領資料と反映状況

| 資料 | 反映 | 備考 |
|---|---|---|
| <テーブル定義・既存画面のパック・プロトタイプ等> | 反映済み / 未 | |

---

## 1. 目的と責務

### 1-1. 目的

1. <この画面・機能が達成すること。1 項目 1 文>

### 1-2. やらないこと(責務の境界)

- <他の画面・処理に任せること。誤って実装しないための線引き>

---

## 2. 全体の中での位置づけ

<!-- 上流・下流の画面や処理との関係。データの流れる向きで描く -->

\`\`\`mermaid
flowchart LR
    A[上流の画面・処理] -->|作るデータ| B[本機能]
    B -->|作るデータ| C[下流の画面・処理]
\`\`\`

---

## 3. データモデル

### 3-1. ER(本機能が扱う範囲)

\`\`\`mermaid
erDiagram
    TABLE_A ||--o{ TABLE_B : "関係の意味"
    TABLE_A {
        string KEY_COL PK
        string COL "意味"
    }
\`\`\`

### 3-2. テーブル・ファイル一覧

| テーブル / ファイル | 用途 | 読み / 書き | 主キー | 備考(世代・状態列など) |
|---|---|---|---|---|
| | | | | |

### 3-3. キー・採番・突き合わせの規則

<!-- 生成規則、NULL の扱い、既存行との突き合わせキー。例外があれば実例を添えて -->

---

## 4. 画面・入出力

### 4-1. 画面構成

<!-- レイアウトの言葉による説明、またはコントロール一覧(名前: 型 — 役割) -->

### 4-2. 入力項目と検証

| 項目 | 型 | 必須 | 検証・制約 | 既定値 |
|---|---|---|---|---|
| | | | | |

### 4-3. 操作(ボタン・メニュー)と活性条件

| 操作 | 動作の概要 | 活性条件 |
|---|---|---|
| | | |

---

## 5. 処理仕様

### 5-1. 状態遷移

\`\`\`mermaid
stateDiagram-v2
    [*] --> 初期
    初期 --> 入力中 : 操作
    入力中 --> 保存済み : 保存
\`\`\`

### 5-2. 処理フロー(主要な処理ごとに 1 図)

#### <処理名>

\`\`\`mermaid
flowchart TD
    S([開始]) --> A[前提チェック]
    A -->|NG| E[メッセージ表示して中止]
    A -->|OK| B[本処理]
    B --> F([終了])
\`\`\`

- 入力: / 出力: / 例外時:

### 5-3. トランザクション・排他・監査列

<!-- 1 トランザクションの範囲、更新者・更新日時の付け方、同時更新への対処 -->

---

## 6. ファイル構成と責務

| ファイル | 役割 | 新規 / 変更 | 備考 |
|---|---|---|---|
| | | | |

---

## 7. 判断ポイントと根拠

<!-- 迷った点と、採った案・捨てた案。後から読む人が「なぜ」を追えるように -->

| # | 判断 | 採用した案 | 却下した案と理由 | 状態 |
|---|---|---|---|---|
| | | | | 確定 / 仮 |

---

## 8. エラー処理・0 件時の方針

| 状況 | 振る舞い | 表示メッセージ |
|---|---|---|
| | | |

---

## 9. 実装バッチ計画

<!-- 1 バッチ = 1 チャットで完結する大きさ。完了条件は動作確認の観点で書く -->

| バッチ | 内容 | 完了条件 | 着手前に回答が要る確認事項 | 状態 |
|---|---|---|---|---|
| B-1 | | | | 未 |

---

## 10. 確認事項

<!-- 種別: 必須 = 回答がないと実装に進めない / 後回し = 実装を進めながら確認期限(バッチ)までに回答をもらう。
     未回答の件数を状態行の blocking / deferred に反映する。回答が付いたら状態を「回答済み」にし本文へ反映する -->

| # | 種別 | 内容 | 確認期限 | 状態 | 回答 |
|---|---|---|---|---|---|
| A | 必須 | | 設計確定前 | 未 | |
| B | 後回し | | B-n 着手前 | 未 | |

---

## 11. 決定事項

| 日付 | 決定 | 理由 |
|---|---|---|
| | | |

---

## 12. 更新履歴

| 版 | 日付 | 変更内容 |
|---|---|---|
| v0.1 | YYYY/MM/DD | 初版 |

<!-- 記入指示(この HTML コメント群)は完成した文書から削除する。該当しない章は消さず「該当なし」と書く -->
`;

const SPEC_TEMPLATE = `# <画面名または機能名> 仕様書

最終更新: YYYY/MM/DD
対象: <ソリューション名 / プロジェクト名>
この文書は**実装済みの振る舞い**の正本です。設計書(docs/design/)は開発の進め方を記したもので、両者が食い違うときは本書が優先します。

---

## 1. 概要

<!-- 専門用語を使わず 1〜3 文で。初めて読む人がこの章だけで「何をする画面か」を掴めるように -->

- 何をするための画面・機能か:
- 誰が、どんなときに使うか:
- 前提(接続先、権限、事前に必要なデータ):

---

## 2. 用語

| 用語 | 意味 |
|---|---|
| | |

---

## 3. 画面と操作

### 3-1. 画面構成

<!-- 領域ごとに何が表示されるか。コントロール名と画面上の表示名を対応させる -->

### 3-2. 操作一覧

| 操作(ボタン等) | 動作 | 活性条件 |
|---|---|---|
| | | |

### 3-3. 入力項目

| 項目 | 型 | 必須 | 検証・制約 | 既定値 |
|---|---|---|---|---|
| | | | | |

---

## 4. 機能一覧

| ID | 機能 | 概要 | 実装状況 |
|---|---|---|---|
| F-1 | | | 実装済み / 未 |

---

## 5. 処理フロー(機能ごと)

### F-1 <機能名>

\`\`\`mermaid
flowchart TD
    S([開始]) --> A[入力チェック]
    A -->|NG| E[メッセージ表示して中止]
    A -->|OK| B[本処理]
    B --> F([終了])
\`\`\`

- 入力:
- 出力(画面表示・DB 更新・ファイル):
- 例外時:

---

## 6. データ

| テーブル / ファイル | 読み / 書き | 主な列と意味 | 備考 |
|---|---|---|---|
| | | | |

---

## 7. 業務ルール・制約

<!-- 「〜のときは〜する」の形で。根拠(設計書の §、決定事項)があれば添える -->

-

---

## 8. エラー時・0 件時の振る舞い

| 状況 | 表示 / 動作 |
|---|---|
| | |

---

## 9. 設計からの意図的な逸脱

<!-- 設計書どおりに作らなかった箇所。理由を残す(次の開発者と AI が設計書に戻してしまわないように) -->

| 設計書の該当箇所 | 実装した振る舞い | 理由 |
|---|---|---|
| | | |

---

## 10. 未実装・既知の制限

-

---

## 11. 更新履歴

| 日付 | 変更内容 | 対応バッチ |
|---|---|---|
| | | |

<!-- 記入指示(この HTML コメント群)は完成した文書から削除する。該当しない章は消さず「該当なし」と書く -->
`;

const HANDOFF_TEMPLATE = `# 引継ぎ書 — <画面名または機能名>

最終更新: YYYY/MM/DD
設計書: docs/design/<ファイル名>.md / 仕様書: docs/spec/<ファイル名>.md

<!-- この文書はチャット間の申し送りだけを書く。恒常的な知識は設計書・仕様書へ移す。
     書かないこと: 出力規約や作業手順のコピー(ツールが毎回付ける)、コードの関数一覧(パックが正)、添付依頼の文面 -->

## 0. 次のチャットで最初にやること

1. <3〜5 項目。順番どおりに実行できる粒度で>

## 1. 到達点

| バッチ | 内容 | 状態 |
|---|---|---|
| B-1 | | 完了 / 着手中 / 未 |

## 2. 次にやること

- バッチ:
- 手順:
- 着手前に回答が要る確認事項(設計書 §10 の #):

## 3. 未解決事項

| # | 種別(必須 / 後回し) | 内容 | 確認期限 |
|---|---|---|---|
| | | | |

## 4. 未解決のエラーと試したこと(着手中のバッチがあるとき)

<!-- バッチの途中で引き継ぐときだけ書く。完了して引き継ぐなら「該当なし」 -->

- エラーの内容(メッセージ・発生箇所・再現手順):
- 試して効かなかった修正(何を変えて、どうなったか):
- まだ試していない案:

## 5. 踏んだ罠(再発防止)

| 事象 | 原因 | 対処 |
|---|---|---|
| | | |

## 6. 設計からの逸脱

- <仕様書 §9 に反映済みかを明記>

## 7. 動作確認の記録

- <確認したデータ・条件・結果。次のチャットで再確認が要るものを明示>

## 8. 次のチャットに添付が必要な資料

- パック(slnmix の出力)は毎回添付する。それ以外に要るもの:
`;

export const DOC_TEMPLATES: Readonly<Record<DocKind, string>> = {
	design: DESIGN_TEMPLATE,
	spec: SPEC_TEMPLATE,
	handoff: HANDOFF_TEMPLATE,
};

/** slnmix --init-docs が docs/README.md として書く説明文 */
export const DOCS_README = `# docs/ — AI チャットと共有するプロジェクト文書

このフォルダは slnmix(\`--init-docs\`)が作成しました。slnmix はここにある文書を
パック(repomix-output.xml)の \`<docs>\` に含め、文書の状態から作業モードを自動で
決めます。文書の作成・更新は AI が changes.md で出し、petari が保存します。

| 場所 | 文書 | 役割 | 更新のタイミング |
|---|---|---|---|
| \`design/<名前>.md\` | 設計書 | 開発の進め方と判断の根拠。1 行目の状態行(\`<!-- slnmix design: status=... -->\`)で draft / ready を管理 | 設計の質疑応答ごと。必須の確認事項がゼロになったら ready |
| \`spec/<名前>.md\` | 仕様書 | 実装済みの振る舞いの正本。誰が読んでも分かる書き方 | 各バッチの完了時 |
| \`HANDOFF.md\` | 引継ぎ書 | チャット間の申し送りだけ(1 ファイル固定。履歴は git) | チャットを切り替えるとき |
| \`templates/<種別>.md\` | ひな型の上書き(任意) | 内蔵ひな型の代わりに使う(design / spec / handoff) | 必要なときだけ |

## 日常の流れ

\`\`\`text
npx slnmix          # パックと本文用テキスト(.prompt.md)を作る。モードは文書の状態から自動
                    #   設計書なし / draft → design(設計書を質疑応答で仕上げる)
                    #   設計書 ready       → full(実装。引継ぎ書があれば続きから)
npx petari          # AI の changes.md(コード・文書の両方)を適用する
\`\`\`

Copilot Chat には .xml を添付し、.prompt.md の中身を本文に貼ります。
AI は回答の末尾に「継続判定」を付け、引継ぎを勧めたら HANDOFF.md を書き直す
changes.md を出します。
`;

/** slnmix.config.json に --init-docs が書く docs 設定の既定値 */
export const DEFAULT_DOCS_CONFIG = {
	design: "docs/design",
	spec: "docs/spec",
	handoff: "docs/HANDOFF.md",
} as const;

/** ひな型の上書きファイルを置くディレクトリ(ルート相対。固定) */
export const DOC_TEMPLATES_DIR = "docs/templates";
